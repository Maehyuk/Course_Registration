<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.finalexam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalExam">
        <activity android:name=".CalendarActivity"></activity>
        <activity android:name=".EssayActivity" />
        <activity android:name=".WriteActivity" />
        <activity android:name=".CommunityActivity" />
        <activity android:name=".MiridamgiList" />
        <activity android:name=".SincheongList" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".LoginErrorActivity" />
        <activity android:name=".DeleteActivity" />
        <activity android:name=".PlusActivity" />
        <activity android:name=".MypageActivity" />
        <activity android:name=".SugangActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>